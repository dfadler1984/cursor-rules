{
  "name": "cursor-rules",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "changeset": "changeset",
    "version:changesets": "npx -y changeset version && node -e \"const fs=require('fs');const v=require('./package.json').version;fs.writeFileSync('VERSION', v+'\\n');require('child_process').execSync('git add VERSION',{stdio:'inherit'})\""
  }
}