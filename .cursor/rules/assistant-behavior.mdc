description: Default behavior and intent routing for assistant
triggers: ["@behavior", "how do you work"]
lastReviewed: 2025-09-18
healthScore: green

## Process

1. Intent Routing: Parse input (e.g., "/specify" → spec-driven.mdc); default to clarify if ambiguous.
2. Execute: Apply matching rule; log to assistant-learning-log.mdc.
3. Reflect: Suggest optimizations; prompt for feedback.

- Vibe: "@behavior; explain and adapt."

## Config

```json
// .cursor/config.json
{
  "assistantBehavior": {
    "defaultLog": "assistant-learning-log.mdc"
  }
}
```
